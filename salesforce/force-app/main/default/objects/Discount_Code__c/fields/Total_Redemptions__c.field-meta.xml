<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Redemptions__c</fullName>
    <description>Every order this code has been used on: paid card orders plus orders awaiting a check. This is the number the order form checks Max Redemptions against, and the one to read when asking how many people have used a code. Times Redeemed counts only what has been paid for, and a check order sits outside it for as long as the check is in the post.</description>
    <formula>NULLVALUE(Times_Redeemed__c, 0) + NULLVALUE(Check_Redemptions__c, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>How many times this code has been used: paid orders plus orders placed with a check promised. This is the redemption count, and what Max Redemptions is measured against.</inlineHelpText>
    <label>Total Redemptions</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
