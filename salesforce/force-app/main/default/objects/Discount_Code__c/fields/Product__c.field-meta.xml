<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Product__c</fullName>
    <description>Which order form the code is good for. A code is only accepted by the product named here, so a Hospitality Guide code cannot be redeemed against something else later. Leave blank only for a code deliberately valid everywhere.</description>
    <inlineHelpText>Which order form this code works on. Leave blank only if it should work everywhere.</inlineHelpText>
    <label>Product</label>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>hospitality-guide</fullName>
                <default>true</default>
                <label>Hospitality Guide</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
