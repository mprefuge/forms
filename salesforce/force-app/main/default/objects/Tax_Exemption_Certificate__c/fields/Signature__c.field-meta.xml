<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Signature__c</fullName>
    <description>The signature as the signer typed it. Kept separate from Signer Name so the two can be compared: a certificate where they do not match is one worth a second look before the exemption is relied on.</description>
    <inlineHelpText>The signature as typed. Compare it against Signer Name.</inlineHelpText>
    <label>Signature</label>
    <length>121</length>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <type>Text</type>
</CustomField>
