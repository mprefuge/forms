<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Organization_Name__c</fullName>
    <description>The purchaser's name exactly as it was typed on the certificate. Kept as free text alongside the Account lookup because the certificate has to record what the buyer attested to, not what somebody later decided the account should be called.</description>
    <inlineHelpText>The purchaser name as the buyer typed it on the certificate.</inlineHelpText>
    <label>Organization Name</label>
    <length>255</length>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <type>Text</type>
</CustomField>
